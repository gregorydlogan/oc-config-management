todos:

matterhorn user name (could be other than matterhorn)
cleanup APT lines
proper variables (different build profiles, etc)
apache
multiple solrs
support for NFS that's not on the admin server
build from scratch (deploying ssh keys?)
pushing solr schema around rather than cloning with git (push from admin?  Needs internal ssh keys.)
